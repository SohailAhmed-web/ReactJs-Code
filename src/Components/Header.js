import React from "react"; 
import "./Header.css"

const Header = () => {
const headerStyle = {
  border: "2px solid red",
  padding: "2mvx",
  backgroundColor:"red",
};

  return <nav
  style={headerStyle}>Header</nav>
}

export default Header
